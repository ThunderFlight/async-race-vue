export default interface Car {
  name: string;
  color: string;
  id: number;
}

export default interface Engine {
  velocity: number;
  distance: number;
}

export default interface Drive {
  success: boolean;
}
